# SLR_Project
A simple linear regression model that predicts a son's height based on the father's height. Built with Flask, Scikit-learn, and HTML/CSS, this project demonstrates model deployment on Render with an interactive web interface.
